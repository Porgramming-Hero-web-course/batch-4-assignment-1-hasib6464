{
    // 

function sumArray(numbers:number[]){
let total=0;
for(let num of numbers){
    total=total+num;
}
return total;
}

const numbers=[1,2,3,4,5,9]
console.log(`the number of ${numbers}`)
console.log(`the number of ${sumArray(numbers)}`)













    // 
}